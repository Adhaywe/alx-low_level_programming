#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: input number one
 * @b: input number two
 * Return: multiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
